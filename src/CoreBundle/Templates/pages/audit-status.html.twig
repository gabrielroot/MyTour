<div class="d-flex flex-column" style="max-width: 167px">
    <div class="mb-1 text-black badge badge-outline-{{ entity.isActive() ? 'success' : 'danger' }}" style="font-size: 9px; padding: 1px">
        <span>{{ entity.isActive() ? 'Ativo' : 'Deletado' | upper }}</span>
    </div>
    <div class="d-flex font-monospace" style="font-size: 10px">
        <div class="d-flex flex-column me-1">
            <span class="fw-bold text-black">Criado:</span>
            <span class="fw-bold text-success">Atualizado:</span>
            {% if not entity.isActive() %}
                <span class="fw-bold text-danger">Deletado:</span>
            {% endif %}
        </div>
        <div class="d-flex flex-column">
            <span class="text-black text-nowrap">{{ entity.createdAt | date('d/m/y - H:i') }}</span>
            <span class="text-success text-nowrap">{{ entity.updatedAt | date('d/m/y - H:i') }}</span>
            {% if not entity.isActive() %}
                <span class="text-danger text-nowrap">{{ entity.deletedAt | date('d/m/y - H:i') }}</span>
            {% endif %}
        </div>
    </div>
</div>