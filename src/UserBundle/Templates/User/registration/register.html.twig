<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Registrar | MyTour</title>
    <!-- plugins:css -->
    <link rel="stylesheet" href="{{ asset('template_assets/vendors/mdi/css/materialdesignicons.min.css') }}">
    <link rel="stylesheet" href="{{ asset('template_assets/vendors/flag-icon-css/css/flag-icons.min.css') }}">
    <link rel="stylesheet" href="{{ asset('template_assets/vendors/css/vendor.bundle.base.css') }}">
    {{ encore_entry_link_tags('app') }}
    <!-- endinject -->
    <!-- Layout styles -->
    <link rel="stylesheet" href="{{ asset('template_assets/css/vertical-light-layout/style.css') }}">
    <!-- End layout styles -->
    <link rel="shortcut icon" href="{{ asset('template_assets/images/favicon.png') }}" />
</head>
<body>
<div class="container-scroller">
    <div class="container-fluid page-body-wrapper full-page-wrapper">
        <div class="content-wrapper d-flex align-items-center auth">
            <div class="row flex-grow">
                <div class="col-lg-5 mx-auto">
                    <div class="auth-form-light text-left p-5">
                        <div class="brand-logo">
                            <img src="{{ asset('template_assets/images/logo-dark.svg') }}">
                        </div>
                        <h4>Novo aqui?</h4>
                        <h6 class="mb-4 font-weight-light">É fácil cadastrar-se. Requer apenas alguns passos...</h6>

                        {{ include ('@CoreBundle/components/global_errors_form.html.twig', {form: form}) }}

                        {{ form_start(form) }}
                            {{ form_widget(form._token) }}
                            <div class="form-group">
                                <div class="form-row">
                                    {{ form_row(form.name) }}
                                </div>
                                <div class="form-row">
                                    {{ form_row(form.username) }}
                                </div>
                                <div class="form-row">
                                    {{ form_row(form.birthday) }}
                                </div>
                                <div class="form-row">
                                    {{ form_row(form.plainPassword) }}
                                </div>
                            </div>
                            <div class="mt-3 d-grid">
                                <button
                                    type="submit"
                                    class="btn d-grid btn-primary btn-lg font-weight-medium auth-form-btn"
                                >
                                    CADASTRAR
                                </button>
                            </div>
                            <div class="text-center mt-4 font-weight-light">
                                Já possui uma conta?
                                <a href="{{ path('login_index') }}" class="text-primary">Entrar</a>
                            </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
        <!-- content-wrapper ends -->
    </div>
    <!-- page-body-wrapper ends -->
</div>
<!-- container-scroller -->
<script src="{{ asset('template_assets/vendors/js/vendor.bundle.base.js') }}"></script>
<!-- endinject -->

<!-- inject:js -->
<script src="{{ asset('template_assets/vendors/select2/select2.min.js') }}"></script>

{% block stylesheets %}
    {{ encore_entry_link_tags('app') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>